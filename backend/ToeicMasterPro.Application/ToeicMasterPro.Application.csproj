<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\ToeicMasterPro.Domain\ToeicMasterPro.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Chỉ giữ package thực sự có code dùng tới.
      Đã gỡ (2026-07-26): FluentValidation, Mapster, MediatR — cài từ đầu dự án
      nhưng không có file nào dùng. Validate hiện làm bằng DataAnnotations +
      kiểm tra trong Service; mapping viết tay trong Service; không dùng CQRS.
    -->
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="8.0.2" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
